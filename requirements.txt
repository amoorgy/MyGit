# MyGit - Rust Dependencies
# This file documents the Rust crate dependencies for the project
# Actual dependency management is done via Cargo.toml

# Async Runtime
tokio = "1.35" (features: full)

# HTTP Client
reqwest = "0.11" (features: json, stream)

# Serialization
serde = "1.0" (features: derive)
serde_json = "1.0"
toml = "0.8"

# CLI
clap = "4.4" (features: derive)
colored = "2.1"
dialoguer = "0.11" (features: fuzzy-select)

# Terminal UI
ratatui = "0.25"
crossterm = "0.27"
tui-input = "0.8"

# Git Operations
git2 = "0.18"

# Error Handling
anyhow = "1.0"
thiserror = "1.0"

# Utilities
shell-words = "1.1"
directories = "5.0"
chrono = "0.4"
futures = "0.3"
async-trait = "0.1"
tokio-util = "0.7"

# OAuth (GitHub/GitLab)
oauth2 = "4.4"
axum = "0.7"
tower-http = "0.5" (features: cors)

# Development Dependencies
[dev]
tempfile = "3.9"

# System Requirements
# - Rust 1.70 or later (installed: 1.92.0)
# - Cargo (Rust package manager)
# - Git (for git2 crate)
# - OpenSSL or similar SSL library (for HTTPS)
# - Windows: MSVC build tools or GNU toolchain

# Build Instructions
# 1. Install Rust: https://rustup.rs/
# 2. Clone the repository
# 3. Run: cargo build (development) or cargo build --release (production)
# 4. Run: cargo run -- [args] (to run the application)

# Testing
# Run: cargo test

# Installation
# Run: cargo install --path .
